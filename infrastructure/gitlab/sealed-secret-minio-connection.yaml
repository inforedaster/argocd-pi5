# PLACEHOLDER — generate and replace with a real SealedSecret before syncing.
#
# 1. Create connection.yaml with your MinIO credentials:
#
#    provider: AWS
#    region: us-east-1
#    aws_access_key_id: <MINIO_ROOT_USER>
#    aws_secret_access_key: <MINIO_ROOT_PASSWORD>
#    aws_signature_version: 4
#    host: school-minio.shared.svc.cluster.local
#    endpoint: http://school-minio.shared.svc.cluster.local:9000
#    path_style: true
#
# 2. Run:
#    kubectl create secret generic gitlab-minio-connection \
#      --namespace gitlab \
#      --from-file=connection=connection.yaml \
#      --dry-run=client -o yaml \
#    | kubeseal --format yaml > infrastructure/gitlab/sealed-secret-minio-connection.yaml
#
# Then commit the resulting SealedSecret in place of this file.
