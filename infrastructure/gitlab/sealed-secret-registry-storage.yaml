# PLACEHOLDER — generate and replace with a real SealedSecret before syncing.
#
# 1. Create config.yaml with your MinIO credentials:
#
#    s3:
#      bucket: gitlab-registry
#      accesskey: <MINIO_ROOT_USER>
#      secretkey: <MINIO_ROOT_PASSWORD>
#      region: us-east-1
#      regionendpoint: http://school-minio.shared.svc.cluster.local:9000
#      pathstyle: true
#      v4auth: true
#
# 2. Run:
#    kubectl create secret generic gitlab-registry-storage \
#      --namespace gitlab \
#      --from-file=config=config.yaml \
#      --dry-run=client -o yaml \
#    | kubeseal --format yaml > infrastructure/gitlab/sealed-secret-registry-storage.yaml
#
# Then commit the resulting SealedSecret in place of this file.
