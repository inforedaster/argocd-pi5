# PLACEHOLDER — generate and replace with a real SealedSecret before syncing.
#
# 1. Choose a strong password (min 8 chars).
# 2. Run:
#    kubectl create secret generic gitlab-initial-root-password \
#      --namespace gitlab \
#      --from-literal=password='<YOUR_PASSWORD>' \
#      --dry-run=client -o yaml \
#    | kubeseal --format yaml > infrastructure/gitlab/sealed-secret-root-password.yaml
#
# Then commit the resulting SealedSecret in place of this file.
